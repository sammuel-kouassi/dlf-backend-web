spring.application.name=dlf_web_backend

spring.datasource.url=jdbc:postgresql://localhost:8090/dlf_backend
spring.datasource.username=postgres
spring.datasource.password=z8Jf14yNeUgPQJcLnnVKGpX0yK3jaCkT

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.properties.hibernate.globally_quoted_identifiers=false

logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=DEBUG
server.port=8081


# ─── JWT ───────────────────────────────────────────────────────
# Secret : 32+ caractères — ne pas committer en production
app.jwt.secret=DLF_S3cr3tK3y_Ch4ng3zM0iEn_Pr0duction_2026!XYZ
# Durée : 24h en millisecondes
app.jwt.expiration-ms=86400000

# ─── Actuator ──────────────────────────────────────────────────
management.endpoints.web.exposure.include=health,info


# ─── Upload fichiers ───────────────────────────────────────────
# Dossier de stockage — chemin absolu recommandé en production
app.upload.dir=./uploads/images
# URL publique du serveur — utilisée pour construire les URLs des images
# Remplacez par votre IP/domaine réel en production
app.base-url=http://192.168.1.4:8081
# Taille max par fichier (10 MB) et par requête (50 MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB